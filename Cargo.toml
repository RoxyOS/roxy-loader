[workspace]
members = ["image-builder","runner"]

[package]
name = "roxy-loader"
version = "0.1.0"
edition = "2024"

[dependencies]
uefi = { version = "0.37.0", features = ["panic_handler"] }

[workspace.dependencies]
image-builder.path = "./image-builder"
