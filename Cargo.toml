[workspace]
members = ["basic-kernel","image-builder", "xtask"]

[package]
name = "roxy-loader"
version = "0.1.0"
edition = "2024"

[dependencies]
elfloader = "0.16.0"
uefi = { version = "0.37.0", features = ["panic_handler"] }

[workspace.dependencies]
image-builder.path = "./image-builder"
